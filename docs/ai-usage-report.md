# AI Usage Report

AI TOOL USED: Claude

## WHAT I ASKED AI TO DO:
1. Explain the assignment brief and help me choose which of the 5 problems to attempt, given my DevOps background and time constraint.
2. Define a Phase 1 plan (objective, MVP scope, roles, workflow, tech stack, project structure) for the Student Support & Ticket Management assignment.
3. Guide me step-by-step through setting up the project (Node.js + Express + SQLite backend, CRUD API for tickets, a minimal HTML/JS frontend, and this documentation), verifying each step before moving to the next.

## PROMPT THAT WAS MOST USEFUL:
Asking it to define the Phase 1 plan before any coding started — covering objective, MVP scope, roles, workflow, features, assumptions, stack, and folder structure — because it gave a checklist against the original assignment brief that every later phase was built against.

## CODE GENERATED BY AI:
Nearly all of it: the Express app setup (`server/app.js`), the SQLite schema and connection (`server/db/database.js`), all API routes (`server/routes/health.js`, `tickets.js`, `users.js`) including create/list/detail/update endpoints and the overdue-ticket filter, and the frontend pages (`public/index.html`, `public/ticket.html`).

## CODE I MODIFIED:
[Fill this in honestly based on what you actually changed — e.g., if you adjusted styling, added a feature, fixed a bug yourself, or left the generated code as-is, state that directly. Example: "None — used the generated code as provided" or "Adjusted the SLA hour values for priority levels" if you did.]

## AI OUTPUT THAT WAS WRONG:
The `package.json` "scripts" section became invalid JSON at one point during manual editing in the editor (extra quotes/nesting), which broke `npm run dev` until corrected.

## HOW I IDENTIFIED THE PROBLEM:
Ran `node -e "console.log(require('./package.json').scripts)"` to validate the JSON, which would have thrown a parse error if the file were still malformed — combined with visually inspecting the file content via `type package.json`.

## HOW I FIXED IT:
Replaced the entire `package.json` content with a corrected, valid version and re-verified with the same Node command before proceeding.
